/*
A palindromic number reads the same both ways.
The largest palindrome made from the product of two 2-digit numbers is 9009 = 91 × 99.

Find the largest palindrome made from the product of two 3-digit numbers.
*/

var isPalindrome= function(number){

};

var findNextLargestProduct = function(lastAttemptA, lastAttemptB){

};

var largestPalindromeProduct = function(numberOfDigits){
  var attemptA = Math.pow(10,numberOfDigits) -1 ;
  var attemptB = attemptA;
  var currentLargestPalindrome;
  print("Current attempts : ", attemptA, attemptB);

  //Multiple current attempts
  //Check if palindrome
    //if True, check against currentLargestPalindrome
    //Replace if so
  //If not palidrome,
    // If lastAttemptA>=0 decriment lastAttemptA
      //else lastAttemptB



}

largestPalindromeProduct(3);

